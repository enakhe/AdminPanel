@{
    ViewData["Title"] = "Users";
    Layout = "_Dashboard";
    var errorMessage = ViewData["ErrorMessage"] as string;
}

@using AdminPanel.InputModel
@model UserViewModel

<div class="content-wrapper">
    <div class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h1 class="m-0">Create a new user</h1>
                </div>
                <div class="col-sm-6">
                    <ol class="breadcrumb float-sm-right">
                        <li class="breadcrumb-item"><a href="/">Home</a></li>
                        <li class="breadcrumb-item"><a href="/Admin/Index">Dashboard</a></li>
                        <li class="breadcrumb-item active">Course</li>
                    </ol>
                </div>
            </div>
        </div>
    </div>

    <section class="content">
        <div class="container-fluid">
            @{
                if(!string.IsNullOrEmpty(@errorMessage))
                {
                    <partial name="_StatusMessage" for=@errorMessage />
                }
            }
            <div class="d-flex justify-content-center align-items-center">
                <div class="col-md-10">
                    <div class="card">
                        <div class="card-header bg-dark">
                            <h3 class="card-title text-light">User | Create</h3>

                            <div class="card-tools">
                                <button type="button" class="btn btn-tool" data-card-widget="collapse" title="Collapse">
                                    <i class="fa-solid fa-minus text-light"></i>
                                </button>
                            </div>
                        </div>
                        <div class="card-body">
                            <form id="createForm" enctype="multipart/form-data" method="post">
                                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                                <div class="form-group">
                                    <label for="courseTitle">Username</label>
                                    <input type="text" placeholder="Username" asp-for="@Model.Username" autocomplete="title" aria-required="true" class="form-control shadow-none">
                                </div>
                                <div class="form-group">
                                    <label for="courseCode">First name</label>
                                    <input type="text" placeholder="First name" asp-for="@Model.FirstName" autocomplete="code" aria-required="true" class="form-control shadow-none">
                                </div>
                                <div class="form-group">
                                    <label for="courseDuration">Last name</label>
                                    <input type="text" placeholder="Last name" asp-for="@Model.LastName" autocomplete="duration" aria-required="true" class="form-control shadow-none">
                                </div>
                                <div class="form-group">
                                    <label for="courseAmount">Password</label>
                                    <input type="text" placeholder="Password" asp-for="@Model.Password" autocomplete="amount" aria-required="true" class="form-control shadow-none">
                                </div>
                                <button type="submit" class="w-100 btn btn-lg btn-success">Save</button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
</div>